<%= render 'show' %>

<div class="container">
  <div class="row">
    <div class="col-sm-8">
      <%= render 'new_comment' %>
    </div>

    <div class="col-sm-4">
      <%= render 'comments' %>
      <%= will_paginate @comments %>
   </div>
  </div>
</div>
